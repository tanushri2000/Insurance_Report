spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Tanushri
    url: jdbc:mysql://localhost:3306/ies
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  mail:
    host: smtp.gmail.com
    username: tanushribhandare@gmail.com
    password: tahb twux zfsx qftu
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#tahb twux zfsx qftu
